[pytest]
# Tests import `app.*`, so backend/ must be on the path. Setting it here means
# `pytest` works from the backend directory with no PYTHONPATH juggling.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
